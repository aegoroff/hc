#ifndef HC_GPU_C_H_
#define HC_GPU_C_H_

/*
 * translate-c root for gpu.zig — canonical GPU ABI plus per-algorithm
 * run/prepare entry points (gpu_hashes.h, pulled in by gpu_abi.h).
 */

#include "gpu_abi.h"

#endif /* HC_GPU_C_H_ */
