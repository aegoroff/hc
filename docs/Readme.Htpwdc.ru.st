$appName$ Readme 

$appName$ это простое консольное приложение позволяющее ломать пароли из файлов паролей Apache (.htpasswd) 
методом грубой силы (перебор по словарю). Приложение умеет:

- Ломать все хэши из указанного файла
- Ломать хэш только для определенного логина (передаваемого через соответствующий параметр)
- Выводить список аккаунтов из файла .htpasswd, например чтобы узнать имя аккаунта, для которого делать взлом
- Ломать хэш переданный через командную строку (параметр -h или --hash)

Дополнительно можно:

- определить словарь для подбора (по умолчанию это все цифры и буквы в верхнем и нижнем регистрах)
- определить минимальную длину пароля (по умолчанию 1)
- определить максимальную длину пароля (по умолчанию 10)

Использование

$exeName$ [OPTION] ...

Опции:

  -a [ --dict ] <строка>        словарь пароля. По умолчанию это все цифры и буквы в верхнем и нижнем регистрах
                                в верхнем и нижнем регистрах. Для классов символов можно
                                использовать сокращения. 0-9 все цифры, a-z латинские буквы в нижнем регистре,
                                A-Z латинские буквы в верхнем регистре. Порядок символьных классов не имеет значения.
  -n [ --min ] <число>          установка минимальной длины ломаемого пароля
                                по умолчанию 1
  -x [ --max ] <число>          установка минимальной длины ломаемого пароля
                                по умолчанию 10
  -f [ --file ] <путь>          полный путь к файлу с паролями
  -h [ --hash ] <хэш>           хэш для взлома
  -l [ --login ] <логин>        логин из файла паролей, для которого делать взлом
  -i [ --list ]                 листинг аккаунтов из файла .htpasswd
  -? [ --help ]                 Показать помощь
  
Примеры

Взлом всех логинов из файла

$exeName$.exe -f file.txt


Взлом только логина "login1"

$exeName$.exe -f file.txt -l login1


Список всех логинов файла

$exeName$.exe -f file.txt -i


Взлом всех логинов из файла используя произвольный словарь, минимальную и максимальную длину пароля

$exeName$.exe -f file.txt -a 0123456789 -n 2 -x 5


Взлом всех логинов из файла используя произвольный словарь в сокращенной форме (цифры и латинские буквы в нижнем регистре)

$exeName$.exe -f file.txt -a 0-9a-z


Взлом хэша переданного через командную строку

$exeName$.exe -h {SHA}QL0AFWMIX8NRZTKeof9cXsvbvu8=